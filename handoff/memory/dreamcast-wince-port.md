---
name: dreamcast-wince-port
description: "Goal, assets, and plan for running fuller Windows CE on the Sega Dreamcast (research project)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 70cc9304-2344-468b-9e37-7961fc80e6ec
---

Research project (started 2026-06): get a fuller Windows CE running on a real Sega Dreamcast (SH-4 / SH7091, 16MB RAM @ phys 0x0C000000 / cached 0x8C000000, PowerVR2, AICA, Maple, GD-ROM, optional BBA RTL8139C). Working dir `C:\dev\Dreamcast` (not a git repo).

Two assets:
- `C:\wcedreamcast` = official Sega "Dragon" Dreamcast WinCE SDK v2.1.0.49 = **Windows CE 2.12**, closed-source. Prebuilt kernel at `release/{retail,debug}/0winceos.bin` (packed NK ROM image). Stripped game runtime: multitasking kernel present, NO shell/explorer. Has inc/lib/samples + tools (DUMPNK, bingen, GD Workshop, VCCE).
- `Arquivotheca/WinCE-src_20201004` (GitHub, Oct-2020 MS leak). Branches WINCE300/420/500/600/700/800. Each is `PRIVATE/WINCEOS/...` OS source only — NO PUBLIC tree, NO compiler/build tools. **Key: `WINCE300/.../NK/KERNEL/SHX/` is the SuperH kernel with real `#if defined(SH4)` MMU/INTC/TMU code; SOURCES uses `EXEBASE=0x8C040000` (DC cached RAM base).**

Decisions:
- Target **CE 3.0** (leak floor), not 2.12. Better base, SH-4 kernel present.
- **Path A** (do first): run stock closed kernel + custom EXEs via makeimg + dc-load/BBA. **Path B** (the research): build CE 3.0 from leak, write DC BSP/OAL from scratch (no DC BSP exists), use KallistiOS as hardware reference, marry leak source to Platform Builder 3.0 (has Hitachi SHC compiler). **Path C** (NT 4.0 on SH-4): rejected — NT never had SH HAL/codegen; multi-year; 16MB below NT floor.
- OAL bring-up order: SCIF serial → TMU tick → MMU/cache → KITL → PVR framebuffer → Maple input → shell.

Update (2026-06-23): chose **Path B**. Workspace built at `C:\dev\Dreamcast`: `wince-src/` (WINCE300 shallow clone, single-branch), `dc-bsp/` (our git repo: oal/dreamcast, drivers, inc, files, cesysgen), `docs/01-findings.md` + `docs/02-toolchain-setup.md`, `README.md`.

Key facts learned:
- `DUMPNK` REJECTS `0winceos.bin` ("Invalid NK image header") — it's a **Sega-wrapped** image, not standard CE `.bin`: 0x800 header (magic `D6 1A`), load base `0x0C010000` (DC phys RAM+64KB), payload 0x1CC800; `ECEC` ROMHDR sig at off 2112. Stock 2.12 modules (extracted): full CE core (COREDLL, filesys.exe, gwes.exe, device.exe, ole32, winsock, schannel, sysstart.exe, **Qshell.exe**) + DC drivers (MAPLE, SH4SER, TIMER, DDRAW/DDHAL/DDI/D3DIM, DSOUND/SNDCORE, MPPP/microstk, WATAPI, wsegacd). GWES + minimal shell already present in 2.12.
- Leaked WINCE300 = **WINCEOS only** (NK incl SHX SH-4, CORE, DEVICE, FSDMGR, GWE/MGDI, COMM), ~143 src files, 5.4MB. **No PUBLIC tree, no shell, no build tools.** → NOT buildable alone.
- ~~Hard dependency: Platform Builder 3.0~~ **SUPERSEDED (2026-06-23).** PB 3.0 needs a CD key (won't pirate). Instead use **github.com/gweslab/WindowsCE-Build-Tools** — ships the MS **SH compiler** free (`bin/I386/SH/cl.exe` + c1/c1xx/c2.dll + `shasm.exe`), plus link/nmake/rc/lib and **OAK+DDK headers** (`ce3-oak/INC`). Compiles the leaked SHX kernel + OAL + drivers; no license. PB 3.0 now optional-only. Path B = leaked WINCEOS source ⊕ gweslab SH compiler ⊕ our dc-bsp.
- **MAJOR CORRECTION (2026-06-23): `C:\wcedreamcast` is a COMPLETE makeimg build env** — my earlier "makeimg gap" was wrong (I'd only looked at top of release/retail). It ships: loose kernel `nk.exe`/`nknodbg.exe`/`nkscifkd.exe`; ~90 loose OS module binaries in `release/{retail,debug}/OS|CoreOS_*` (coredll, gwes, filesys, ole32, **shell.exe**, IE stack, all DC drivers); full toolchain in `tools/` (**makeimg.exe, romimage.exe, fmerge.exe, bingen.exe**, regcomp, res2exe, rebaseic); full `.bib`/`.reg`/`.dat` (ce/common/config/platform/**gemini.bib** = 113 mods); `makeimg.out` build log. Image: NK RAMIMAGE @ `8C010000` (=cached 0C010000), RAM @8C600000, DRIVGLOB @8CFD0000, COMPRESSION/KERNELFIXUPS ON. Boot launch order (HKLM\init): gwes(30)→shell.exe(11,debug cmd shell)→wdevice(40)→microstk(50)→**sysstart(60)=title launcher**. **No explorer.exe anywhere** (confirms no desktop shell; gwes IS present so a GUI shell app has a surface).
- **So: NO PB 3.0 needed, NO extraction from packed image, NO custom ROM packer.** Reuse SDK makeimg. Rebuild loop: `wce.bat retail` → edit `.bib`/`.reg` → `makeimg` → `bingen` wrap. gweslab SH compiler only needed to recompile nk.exe (from leaked SHX) or build new modules/apps; `nmake` replaces build.exe.
- Only real gap for *desktop* CE = an **SH-4 explorer.exe/GUI shell** (not in SDK): write a custom launcher on gwes (easiest), port from leaked source, or lift from another SH-4 CE3.x image. Multitasking already in kernel (just CreateProcess multiple apps).
- Path-B first concrete step: makeimg **round-trip** (run unmodified, diff vs shipped `0winceos.bin`) to prove pipeline before changing anything.

**✅ DONE — makeimg round-trip VALIDATED (2026-06-23), zero PB, no compiler needed.** `tools/build_retail.bat` (faithful replica of `wce.bat retail`+`set_imginit`; the sandbox blocks bare-name batch exec from cwd so I replicated env directly) → `makeimg` → `release\retail\NK.bin`: 29 modules, start 8C010000, ROM span 0x1CC43C, errorlevel 0. NK.bin is canonical **B000FF** CE ROM. **DUMPNK now parses NK.bin** (it rejected the wrapped 0winceos.bin) → raw memimage 1,885,244 B, ROMHDR `ECEC` @0x40. Shipped 0winceos.bin has ECEC @0x840 ⇒ **`0winceos.bin = [0x800 Sega hdr] + [raw memimage padded 0x1CC43C→0x1CC800]`**. Wrapper hdr: magic D61A @0x00, load base 0x0C010000 @0x14 (cached 0x8C010000), payload len 0x1CC800 @0x1C. Full chain doc: `docs/03-build-pipeline.md`. Artifacts in `C:\dev\Dreamcast\reference\` (orig + rebuilt NK.bin + rawmem + makeimg.out). Reference 0winceos.bin SHA256 C5C0D53F… (unchanged by makeimg).
**✅ TOOLCHAIN + PROJECT REPO SCAFFOLDED (2026-06-23).** `C:\dev\Dreamcast` is a SHARED workspace also holding OTHER unrelated projects: `sh4pe-toolchain` (user's separate **ReactOS-on-SH4** effort — HANDS OFF), `DreamShell`, `KallistiOS`, `img4dc`. Our WinCE work is now self-contained in **`C:\dev\Dreamcast\wince-dc\`** (git repo, committed 1ebbd8a). Layout: `toolchain/` (setenv.bat, build-image.bat, wrap-image.ps1, unwrap-image.ps1, build-kernel.bat, README), `docs/01-03`, `bsp/`, `reference/MANIFEST.md` (bins gitignored). Externals at top level: `wince-src`, `vendor/WindowsCE-Build-Tools` (gweslab sparse clone bin+ce3-oak+ce3-ddk, 36M).
- **TWO SH-4 PE compilers validated on host**: (1) **gweslab `cl.exe`** (`vendor\WindowsCE-Build-Tools\bin\I386\SH\`) = MS 13.10 Renesas SH, **defaults SH4** (dumpbin → `1A6`) — PRIMARY. (2) authentic **`C:\Windows CE Tools\wce212\bin\SHCL.EXE`** = MS 12.01 Hitachi SH (2.12 vintage) + C1_SH/C1XX_SH/CBE + LINK/LIB/SHASM + WCESH4.BAT — defaults SH3, SH4 flag TBD (NOT /QRsh4) — fallback. (`C:\dev\VC98\bin` exists but EMPTY.)
- **wrap-image.ps1 VERIFIED byte-exact**: rebuilt NK.bin → 0winceos.bin header 0x00–0x2F identical to shipped + exact 1,888,256 size (payload differs = expected). Replaces the old `tools/build_retail.bat` (now `toolchain/build-image.bat`).
**KERNEL BUILD STARTED (2026-06-23, commit 23d2a84).** `build-kernel.bat` compiles leaked NK with SH cc. Key learnings: (1) MUST use pure CE3 include chain, NOT the 2.12 DC SDK inc (it redefines DWORD/BOOL, lacks CE3 consts). Chain: `bsp\inc` ; leak `NK\INC` ; `NK\KERNEL\SHX` ; `CORE\INC` ; gweslab `ce3-oak\INC` (OAK) ; gweslab **`ce3-ppc2k\include`** (base SDK windows/winnt/winbase/windef — had to sparse-add ce3-ppc2k+ce3-hpc2k, ~290M). Defines `-DSH4 -DSHx -DUNDER_CE=300 -D_WIN32_WCE=300 -DUNICODE -DKERNEL`. (2) **Leaked SHx headers are an INCOMPLETE snapshot**: `MEM_SHX.H` missing `VA_SECTION` (other CPUs' MEM_*.H have it); `SECTION_SHIFT`/`CURTLSPTR_OFFSET`/`KINFO_OFFSET` defined nowhere in leak. **Reconstructed in `bsp\inc\mem_shx_patch.h`** (force-included `/FImem_shx_patch.h`): VA_SECTION=25, SECTION_SHIFT=25, CURTLSPTR_OFFSET=0x000 (KSSHX.H lpvTls), KINFO_OFFSET=0x300 (KSSHX.H PendEvents 0x340 = aInfo[KINX_PENDEVENTS=16]). KERNEL.H ERRFALSE asserts re-validate vs C struct. Cleared all 4; errors 50→35.
- The "KWIN32.H header-skew frontier" was RESOLVED: it was a missing **build switch**, not headers. CE3 kernel SOURCES set `-DWINCEOEM=1 -DWINCEMACRO` (gates kfuncs.h→pkfuncs.h/mkfuncs.h) +`-DIN_KERNEL -DDBGSUPPORT`; adding them cleared all 35 at once. pkfuncs.h defines VA_SECTION/SECTION_SHIFT/CURTLSPTR_OFFSET/KINFO_OFFSET identically to our reconstruction → `mem_shx_patch.h` removed as redundant.

**✅ MAJOR PROGRESS (2026-06-23, repo HEAD ~4825bed — REFRESHED; ~30 commits since scaffold):**
- **Kernel core BUILDS from source** → `nkmain.lib` (26 objs: 19 mach-indep C + 4 SHX C + 3 SHX shasm, SH4 0x1A6). Driver `build-nklib.bat`.
- **DC OAL/HAL RECONSTRUCTED** by reversing the shipped SDK `nknodbg.exe` in Ghidra (project `wce`; debug PE's COFF symbols named all 629 funcs). Lives in CE-native tree `vendor/wince-src/PLATFORM/DREAMCAST/KERNEL/HAL/` (fwinit/cfwkatan/fwkatana/ktimer/timer/rtc/oemioctl/isr/mdppfs/oemwdm/debug/…). Decoded DC HW: INTC `0xFFD00004/8/C`, TMU `0xFFD80000`, Holly `SB_IML*` `0xA05F69xx`, KatanaISR2/4/6 demuxers, polled SCIF console. `build-oal.bat`→`oal_dc.lib`.
- **Minimal SH-4 CRT** written (`NK/CRT/SHX/`, fulllibc-PDB-matched names; SDK ships no static libc) → `build-crt.bat`→`crt.lib`.
- **`nk.exe` LINKS from source, 0 unresolved** (`build-nk.bat`, ~264KB, entry StartUp, /base:0x10000 /fixed:no so makeimg rebases to 0x8C040000).
- **IT BOOTS on Flycast**: nk.exe→makeimg→`wrap-image.ps1`→`0winceos.bin`→disc (`make-gdi.ps1` Half-Life-DC pipeline = working path; `make-disc.ps1` CDI also works). Boots through KernelRelocate→CE banner→MMU+cache→OEMInit("Set 4 detected")→"Booting Windows CE 3.00"→first thread. TWO key fixes: (a) plant Flycast "SH-4 Kernel" UTF-16 MMU-magic at VA 0x8C0110A8 (wrap-image.ps1 + StartUp); (b) recover pTOC from ROM sig `ECEC`@RAMIMAGE+0x44 in mdsh3.c SH3Init (romimage mis-patches pTOC when linked at 0x10000). Docs `05-disc-image.md`.
- **CURRENT FRONTIER**: faults loading the FIRST PROCESS — `SC_GetOwnerProcess`/`GetKHeap` TLB-miss on slot-1 mem (~0x03d50000). **ABI probe (`probe-abi.bat` vs shipped PDB via cvdump, `docs/06-userland-abi.md`) PROVED it's NOT an ABI wall**: cross-boundary surfaces (KDataStruct 896, CPUCONTEXT 228/ctx@92, **cinfo/PSL 20**, _HDATA/o32_lite/openexe_t) are byte-identical; only kernel-internal Process/Thread sizes differ (3.0 vs 2.12, benign, self-consistent, ERRFALSE passes). So it's a **kernel loader/VM bug**, debug kernel-side directly (build `debug` for verbose). **Leak CANNOT rebuild full userland** (coredll=dll+lmem only, GWES has NO window/GDI src) → keep stock 2.12 modules. coredll PSL core does build (`coremain.lib`, `build-coredll.bat`).
- New toolchain drivers: build-nklib/oal/crt/nk/coredll, build-asm, probe-abi, make-gdi, make-disc. Docs now 01–06. [Memory long — consolidate older dated entries when convenient.]

**🔬 FIRST-PROCESS FAULT — PRECISELY LOCALIZED via Ghidra MCP (2026-06-24).** MCP now wired properly: `C:\dev\Dreamcast\.mcp.json` runs `uv run --quiet C:\dev\ghidramcp\bridge_mcp_ghidra.py` w/ `GHIDRA_MCP_URL=http://127.0.0.1:8089` (203 tools; Ghidra project `wce` = SDK debug nknodbg.exe @8c000000). Live Flycast trace: **Exc 0x040 (data-TLB-miss read) at PC=8c025520 = `DoImports+0x54`, RA=`LoadOneLibraryPart2+0xf08`, TEA=0x03d50000** (via our nk.map: runtime=link+0x8C000000). Decoded: it's **`coredll.dll` demand-paging** during `SystemStartupFunc`→`LoadOneLibraryW(coredll)` (schedule.c:4842). `DoImports` reads the DLL import descriptor at `BasePtr(R6)=0x03d50000` = XIP `e32_vbase 0x01d50000` + NK slot `0x02000000` (ProcArray[0].dwVMBase=R7) — **BasePtr is CORRECT** (ROM-DLL branch loader.c:2815). Our `ProcessPageFault` (virtmem.c) returns FALSE → unhandled exc when `SectionTable[ixSect]==NULL_SECTION` or block `==NULL_BLOCK`; for 0x03d50000 ixSect=1, ixBlock=0x1d5. **pTOC is CORRECT** (read from our image ROMHDR: dllfirst=0x01b20000 dlllast=0x02000000 nummods=29, brackets 0x01d50000) — so NOT the pTOC patch. DLL region reserved at schedule.c:4836 `VirtualAlloc((LPVOID)first=0x01b20000=slot0, MEM_RESERVE|MEM_IMAGE)`; must alias slot0→current(NK,section1) to land in NKSection (SectionTable[1], set at virtmem.c:397). **Suspect: slot-0→current-process section aliasing / VirtualAlloc section landing.** SDK page-in path = `ProcessPageFault`→`PageInFuncs[]`→`PageInProcess`(8c0263a0, VirtualCopy's ROM page) — machinery exists & correct.
**🎯 ROOT CAUSE FOUND + FIXED (2026-06-24): CE 2.12-vs-3.0 `e32_rom` struct mismatch.** Iterative RETAILMSG instrumentation (DLLrgn@schedule.c:4836, PPF-FAIL@virtmem.c ProcessPageFault, DoImp@loader.c DoImports, PIM-NF@PageInModule) + Ghidra PDB struct query nailed it. Chain: ProcessPageFault reaches valid block (pgr=1,acc=1,aP=0) → calls LoaderPageIn → PageInModule(coredll,0x03d50000) returns 0 = "could not find section" because zaddr=0x01d50000 is the module HEADER page (below s0@rva0x1000). DoImports read it because **`eptr->e32_unit[IMP]={rva=0,size=0x31000}` was WRONG** (coredll has no imports, real IMP={0,0}). **Cause: our 3.0 leak `e32_rom` struct (ROMLDR.H) has `e32_sect14rva`+`e32_sect14size`@+24/+28 and `e32_subsys`@+104 → `e32_unit`@+32, size 106. But the 2.12 modules the SDK romimage writes have `e32_subsys`(ushort)@+24, `e32_unit`@+28, size 100 (NO sect14) — confirmed via Ghidra PDB `e32_rom`=100B + raw image bytes (subsys=9=WINCE_GUI, EXP={0x27800,0x64c4}, IMP={0,0} all align at +28).** LoadE32 read e32_unit shifted +4B → phantom import → unhandled TLB miss.
- **FIX (made, awaiting Flycast confirm):** (1) `vendor/sh-toolchain/ce3-oak/INC/ROMLDR.H` e32_rom → 2.12 layout (e32_subsys ushort before e32_unit; removed e32_sect14rva/size). (2) `loader.c:1701-1702` LoadE32 ROM branch: set `eptr->e32_sect14rva/size=0` (2.12 ROM has no sect14). Rebuilt+reGDI. Expect coredll to load past fault (DoImports returns early on IMP.size==0).
- Likely first of a few 2.12/3.0 struct deltas (ROMHDR + o32_rom already verified OK). Method proven: instrument RETAILMSG → Flycast → Ghidra PDB `get_struct_layout` for authoritative 2.12 layouts.

**✅ e32_rom FIX CONFIRMED + 2nd delta found/fixed (2026-06-24): API method-table holes.** After e32_rom fix: coredll loads ✓, **filesys.exe runs** ✓, real imports parse ✓ (DoImp base=0x10000 imp.sz=0x28). NEXT fault: threads jump to PC=0 via APICall→ObjectCall (KPSLExceptionHandler). OC-NULL probe (added in OBJDISP.C DISPATCH_KERNEL_PSL where `pfn=pci->ppfnMethods[iMethod]`) → **'PROC' API set, method 4 = NULL** (cMethods=9), called by coredll. Cause: **2.12-vs-3.0 API method-numbering delta** — our 3.0 `ProcMthds[4]=(PFNVOID)0` but 2.12 had `SC_ProcGetIndex`. Confirmed via Ghidra: ProcMthds table @SDK 0x8c002220 (found via get_xrefs_to SC_ProcTerminate 0x8c0156d0 DATA xref), read_memory decoded all 9 entries — identical to ours EXCEPT [4]=SC_ProcGetIndex(0x8c01a154). **FIX: schedule.c ProcMthds[4]=SC_ProcGetIndex** (function already exists in kmisc.obj — 3.0 only dropped the table entry; my first attempt added a dup body→LNK2005, removed it, kept table entry+prototype). Rebuilt+reGDI.
- **PATTERN for remaining bring-up:** more API-table holes may surface (other ProcMthds/ThrdMthds/Win32-set slots 3.0 zeroed but 2.12 coredll calls). REACTIVE fix recipe (per OC-NULL): Ghidra get_xrefs_to a known method in the set → DATA xref = the SDK table base → read_memory → diff vs our table → restore the missing SC_ fn. Ghidra MCP tools in use: search_functions, decompile_function, get_struct_layout, search_data_types, get_xrefs_to, read_memory, list_globals.
- **Housekeeping: DCDBG RETAILMSG probes still in loader.c/virtmem.c/schedule.c/OBJDISP.C — strip all once boot is stable.** e32_rom fix is real (ROMLDR.H + loader.c:1701-1702) — KEEP that.
- **Housekeeping: DCDBG RETAILMSG debug edits still in vendor/wince-src (loader.c/virtmem.c/schedule.c) — strip once confirmed.** SDK release\retail\nknodbg.exe = OUR nk.exe (stock at .stock). utils/ip.bin = SDK ip_drago.bin (gitignored, recopy on fresh clone).

3 toolchains (don't conflate): PB3.0 = OS image; eVC3/DC-SDK = CE apps; KallistiOS/sh-elf-gcc = loader+HW reference ONLY (ELF, can't compile CE — CE is SHx PE/COFF). Loaders: dcload-ip/BBA or dcload-serial; master via GD Workshop / makeip+cdi4dc. Emulators: PB CE-SH4 emu (kernel bring-up), Flycast/lxdream (final image).

Next: install PB 3.0 in a WinXP VM, build a stock SH-4 image first, then create PLATFORM\Dreamcast BSP and start OAL bring-up (SCIF print → TMU tick → MMU → KITL → PVR fb → Maple → GD-ROM → shell). See [[user-dreamcast-expertise]].
